var p = document.querySelector(".resultado")





/*function teclaPressionada(e) {
    //p.innerHTML += `${e.code}`
    for(var i = 0; i < 10; i++){
        if(e.code == `Numpad${i}`){
            p.innerHTML += `${i}`
        }
        if(e.code == `Digit${i}`){
            p.innerHTML += `${i}`
        }
    } 
    switch (e.code) {
        case "NumpadMultiply":
            p.innerHTML += `*`
            break;
        case "Backspace":
            p.innerHTML -= `*`
            break;
        
        
        default:
            break;
    }  
    
}*/